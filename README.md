To start app you need to run the following commands at the root of the solution folder:

    - Run `docker-compose build`

    - Run `docker-compose up`
The swagger will be available at http://localhost:500/swagger
